# dependencies
node_modules/

# vercel
.vercel/

# local env / secrets — never commit keys
.env
.env.*
*.local

# editor
.vscode/
.DS_Store

# local key overrides (if you ever keep a real-key copy for `vercel dev`)
config.local.js
dei_config.local.js

# map-generator download cache (SRTM tiles, Natural Earth, Overpass) — regenerable
gallery/bura-ceramics/tools/cache/

